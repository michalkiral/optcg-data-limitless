{
  "comment": "Hand-curated prints Limitless does not list. Same shape as the original optcg-data repo: { id, parent, name?, set?, image?, note? }. Rarely needed now that cards come from Limitless (it already lists the promos/alt-arts Bandai's cardlist lacked). No price-map here — ids and prices come from one source, so there is nothing to map.",
  "prints": []
}
