{
  "name": "optcg-data-limitless",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static, versioned JSON catalog of One Piece Card Game cards AND prices, scraped from Limitless TCG in one crawl and served over jsDelivr. Single source — card ids and prices come from the same place, so nothing needs mapping.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "taxonomy": "node scripts/build.mjs --taxonomy"
  },
  "license": "MIT"
}
